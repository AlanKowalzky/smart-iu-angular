import { ActiveDeviceDirective } from './active-device.directive';

describe('ActiveDeviceDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveDeviceDirective();
    expect(directive).toBeTruthy();
  });
});
